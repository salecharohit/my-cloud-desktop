[Unit]
Description=code-server
After=network.target network-online.target

[Service]
Type=simple
User={{ code_server_user }}
ExecStart={{ code_server_bin_dir }}/code-server --config {{ code_server_data_dir }}/config.yaml
Restart=always

[Install]
WantedBy=multi-user.target
